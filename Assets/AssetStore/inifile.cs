using UnityEngine;
// Temporario para usar como marcador de projeto
// Para saber se o projeto é o mesmo fornecido pelo professor
public class inifile : MonoBehaviour
{
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
